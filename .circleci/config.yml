version: 2.1

jobs:
  run-tests:
    docker:
      - image: cimg/node:14
        auth:
          username: dannyoka
          password: $DOCKERHUB_PASSWORD
    steps:
      - checkout
      - run:
          name: "build docker image"
          command: "echo Hello world!"

workflows:
  test-workflow:
    jobs:
      - run-tests
